# References the docs check must not fail on, one per line.
#
# Only one kind belongs here: **planned** — something named in a plan or an ADR
# before it is built. Delete the entry when the thing lands, so the checker
# starts verifying it for real.
#
# Paths that are absent by design do NOT belong here. The check asks .gitignore
# directly, so a generated or private file is allowed without an entry — that
# is what keeps the answer the same in a working tree and in a clean CI
# checkout. Anything else failing the check is drift: fix the doc.

# --- planned: V2 (ADR-0020, docs/v2-plan.md) ---
int_jobs_structured
int_jobs_scored

# --- planned: only if ADR-0017's gates pass ---
stg_openjobdata__jobs
raw_openjobdata_jobs
