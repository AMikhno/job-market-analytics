# Secrets / private config — never commit values
.env
config/companies.csv
config/companies.active.csv
config/companies.csv.bak
config/discovery/
# Personal runbook: the manual half of the system (gh commands, secrets setup).
# No secret values in it, but it is Ana's notes, not project documentation.
NOTES.local.md
config/profile.yaml
*.key
*.pem
**/keyfile*.json
~/.gcp/

# Data artifacts
*.duckdb
*.duckdb.wal
*.parquet
/data/

# OS / editor / runtime artifacts
.DS_Store
.vscode/
ingest_summary.json

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# dbt (dbt/profiles.yml is committed — it is env-var driven, no secret values)
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
.vscode/
